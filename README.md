# pthreads ape maze simulation
Multi-threaded simulation of ape families collecting bananas in a maze using C and POSIX threads (pthreads) on Linux, modeling concurrent behaviors, synchronization, and shared resource management.
